# Rust build output
/target

# Per machine files the tool writes next to itself, in case it is run from
# the repository root during development.
/settings.toml
/processlist.cfg
/protocol.toml
/config.toml

# Editor and OS noise
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
